---
title: Überprüfung
description: >-
  Überprüfen Sie die GRE-Tunnel- und BGP-Konfiguration auf BIG-IP und beheben
  Sie häufige Probleme.
sidebar:
  order: 6
i18n:
  sourceHash: 696153ba9b9a
  translator: machine
---

## Überprüfung

### BIG-IP

#### Tunnel

Auf jeder Einheit für die eigenen Tunnel ausführen:

```shell title="tmsh"
show net tunnels tunnel xc-ddos-c1t1-v4
show net tunnels tunnel xc-ddos-c1t1-v6
show net tunnels tunnel xc-ddos-c1t2-v4
show net tunnels tunnel xc-ddos-c1t2-v6
show net tunnels tunnel xc-ddos-c2t1-v4
show net tunnels tunnel xc-ddos-c2t1-v6
show net tunnels tunnel xc-ddos-c2t2-v4
show net tunnels tunnel xc-ddos-c2t2-v6
list net tunnels tunnel xc-ddos-c1t1-v4 all-properties
```

#### Self-IPs

```shell title="tmsh"
list net self xc-ddos-*
```

#### Pfadprüfung

Von jeder Einheit:

```shell title="tmsh"
# BIG-IP-A tunnels
ping xXC_C1_T1_INNER_V4x source xBIGIP_C1_T1_INNER_V4x
ping xXC_C2_T1_INNER_V4x source xBIGIP_C2_T1_INNER_V4x

# BIG-IP-B tunnels
ping xXC_C1_T2_INNER_V4x source xBIGIP_C1_T2_INNER_V4x
ping xXC_C2_T2_INNER_V4x source xBIGIP_C2_T2_INNER_V4x
```

#### BGP

In imish auf jeder Einheit:

```shell title="imish"
show ip bgp summary
show ipv6 bgp summary
show ip bgp
show ipv6 bgp

# BIG-IP-A neighbors
show ip bgp neighbors xXC_C1_T1_INNER_V4x
show ip bgp neighbors xXC_C2_T1_INNER_V4x
show ip bgp neighbors xXC_C1_T1_INNER_V4x advertised-routes
show ip bgp neighbors xXC_C1_T1_INNER_V4x received-routes

# BIG-IP-B neighbors
show ip bgp neighbors xXC_C1_T2_INNER_V4x
show ip bgp neighbors xXC_C2_T2_INNER_V4x
show ip bgp neighbors xXC_C1_T2_INNER_V4x advertised-routes
show ip bgp neighbors xXC_C1_T2_INNER_V4x received-routes

show ip route
show ipv6 route
```

:::caution[Checkliste zur Fehlerbehebung]
Wenn BGP keine Verbindung herstellt:

1. Überprüfen Sie, ob der GRE-Tunnel aktiv ist: `show net tunnels tunnel` sollte
   den Status `up` anzeigen.
1. Bestätigen Sie, dass `allow-service` auf der inneren Self-IP `tcp:179` enthält.
1. Pingen Sie die entfernte innere IP-Adresse durch den Tunnel.
1. Überprüfen Sie, ob das BGP-Passwort auf beiden Seiten übereinstimmt (häufige Ursache für
   `NOTIFICATION`-Fehler).
1. Stellen Sie sicher, dass Präfixlisten und Routenzuordnungen korrekt angewendet werden.
1. Prüfen Sie `show ip bgp neighbors <IP>` auf den letzten Fehler-/Rücksetzgrund.
1. Überprüfen Sie auf jeder HA-Einheit, ob die eigenen Tunnel der Einheit aktiv sind und ihre
   eigenen BGP-Nachbarn konfiguriert sind (nicht die der anderen Einheit).
:::

### F5 XC Konsole

Navigieren Sie zu **DDoS and Transit Services >
[DDoS Protection > Visibility](https://docs.cloud.f5.com/docs-v2/ddos-and-transit-services/how-tos/network-firewall/l3l4-ddos-mitigation)**, um Folgendes anzuzeigen:

- Datenverkehr nach Netzwerk, Zone und Anwendung.
- Blockierter im Vergleich zu erlaubtem Datenverkehr.
- Angriffsdetails und Bereinigungsmaßnahmen.
