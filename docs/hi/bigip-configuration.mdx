---
title: BIG-IP कॉन्फ़िगरेशन
description: >-
  BIG-IP HA जोड़ियों पर GRE टनल, सेल्फ़ IP और BGP कॉन्फ़िगर करने के लिए tmsh और
  imish कमांड।
sidebar:
  order: 5
i18n:
  sourceHash: 23019a13550e
  translator: machine
---

## BIG-IP

- (Route Domain 0 उदाहरण)

नीचे दिए गए सभी कमांड BIG-IP पर **tmsh** में चलाए जाते हैं। ऑब्जेक्ट नाम
और IP को आवश्यकतानुसार समायोजित करें।

BIG-IP पर सामान्य GRE टनल कॉन्फ़िगरेशन के लिए, देखें
[Configuring a GRE Tunnel Using BIG-IP](https://community.f5.com/kb/technicalarticles/configuring-a-generic-routing-encapsulation-gre-tunnel-using-big-ip/289030)। Cloud के साथ प्रारंभिक
राउटेड कॉन्फ़िगरेशन सेटअप के लिए, देखें [K000147949](https://my.f5.com/manage/s/article/K000147949)।

### tmsh

```bash title="tmsh"
[root@bigip:Active]# tmsh
root@(bigip)(cfg-sync Standalone)(Active)(/Common)(tmos)#
```

### आउटर सेल्फ़ IP

GRE एंडपॉइंट

ये प्रत्येक BIG-IP यूनिट पर वे IP हैं जो **GRE टनल
एंडपॉइंट** के रूप में उपयोग किए जाते हैं, आमतौर पर एक्सटर्नल VLAN पर। प्रत्येक यूनिट का अपना
नॉन-फ़्लोटिंग आउटर सेल्फ़ IP (`traffic-group-local-only`) होता है:

**BIG-IP-A:**

```shell title="tmsh"
create net self xc-ddos-v4-self-a \
  vlan external \
  traffic-group traffic-group-local-only \
  allow-service add { icmp:any gre:any } \
  address xBIGIP_A_OUTER_V4x/24

create net self xc-ddos-v6-self-a \
  vlan external \
  traffic-group traffic-group-local-only \
  allow-service add { icmp:any gre:any } \
  address xBIGIP_A_OUTER_V6x/64
```

**BIG-IP-B:**

```shell title="tmsh"
create net self xc-ddos-v4-self-b \
  vlan external \
  traffic-group traffic-group-local-only \
  allow-service add { icmp:any gre:any } \
  address xBIGIP_B_OUTER_V4x/24

create net self xc-ddos-v6-self-b \
  vlan external \
  traffic-group traffic-group-local-only \
  allow-service add { icmp:any gre:any } \
  address xBIGIP_B_OUTER_V6x/64
```

### GRE टनल

प्रत्येक टनल एक BIG-IP यूनिट से Cloud
स्क्रबिंग सेंटर एंडपॉइंट की ओर इंगित करती है। **प्रति यूनिट दो टनल** बनाएं (प्रत्येक
भौगोलिक स्थान वाले स्क्रबिंग सेंटर के लिए एक) जिससे HA जोड़ी में कुल **चार लॉजिकल
टनल** बनें:

**Tunnel C1-T1 — BIG-IP-A से xCENTER_1x:**

```shell title="tmsh"
create net tunnels tunnel xc-ddos-c1t1-v4 \
  local-address xBIGIP_A_OUTER_V4x \
  profile gre \
  remote-address xXC_C1_OUTER_V4x

create net tunnels tunnel xc-ddos-c1t1-v6 \
  local-address xBIGIP_A_OUTER_V6x \
  profile gre \
  remote-address xXC_C1_OUTER_V6x
```

**Tunnel C2-T1 — BIG-IP-A से xCENTER_2x:**

```shell title="tmsh"
create net tunnels tunnel xc-ddos-c2t1-v4 \
  local-address xBIGIP_A_OUTER_V4x \
  profile gre \
  remote-address xXC_C2_OUTER_V4x

create net tunnels tunnel xc-ddos-c2t1-v6 \
  local-address xBIGIP_A_OUTER_V6x \
  profile gre \
  remote-address xXC_C2_OUTER_V6x
```

**Tunnel C1-T2 — BIG-IP-B से xCENTER_1x:**

```shell title="tmsh"
create net tunnels tunnel xc-ddos-c1t2-v4 \
  local-address xBIGIP_B_OUTER_V4x \
  profile gre \
  remote-address xXC_C1_OUTER_V4x

create net tunnels tunnel xc-ddos-c1t2-v6 \
  local-address xBIGIP_B_OUTER_V6x \
  profile gre \
  remote-address xXC_C1_OUTER_V6x
```

**Tunnel C2-T2 — BIG-IP-B से xCENTER_2x:**

```shell title="tmsh"
create net tunnels tunnel xc-ddos-c2t2-v4 \
  local-address xBIGIP_B_OUTER_V4x \
  profile gre \
  remote-address xXC_C2_OUTER_V4x

create net tunnels tunnel xc-ddos-c2t2-v6 \
  local-address xBIGIP_B_OUTER_V6x \
  profile gre \
  remote-address xXC_C2_OUTER_V6x
```

टनल नाम (`xc-ddos-c1t1-v4`, आदि) मनमाने हैं; अपनी स्वयं की
नामकरण परंपरा का उपयोग करें।

### टनल MTU सेट करें

GRE एनकैप्सुलेशन ओवरहेड जोड़ता है (IPv4 आउटर के लिए 24 बाइट्स, IPv6
आउटर के लिए 44 बाइट्स)। स्पष्ट MTU के बिना, 1500 बाइट्स के करीब पैकेट
फ़्रैगमेंट होंगे या ड्रॉप हो जाएंगे। एनकैप्सुलेशन ओवरहेड को ध्यान में रखते हुए
टनल MTU सेट करें:

```shell title="tmsh"
modify net tunnels tunnel xc-ddos-c1t1-v4 mtu 1476
modify net tunnels tunnel xc-ddos-c1t1-v6 mtu 1456
modify net tunnels tunnel xc-ddos-c1t2-v4 mtu 1476
modify net tunnels tunnel xc-ddos-c1t2-v6 mtu 1456
modify net tunnels tunnel xc-ddos-c2t1-v4 mtu 1476
modify net tunnels tunnel xc-ddos-c2t1-v6 mtu 1456
modify net tunnels tunnel xc-ddos-c2t2-v4 mtu 1476
modify net tunnels tunnel xc-ddos-c2t2-v6 mtu 1456
```

:::caution[पाथ MTU]
ऊपर दिए गए मान आउटर नेटवर्क पर 1500-बाइट पाथ MTU मानते हैं।
यदि आपका अपस्ट्रीम पाथ MTU छोटा है तो समायोजित करें। सुनिश्चित करें कि ICMP
"destination unreachable / fragmentation needed" (type 3, code 4)
किसी भी मध्यवर्ती डिवाइस पर **ब्लॉक नहीं** है ताकि Path MTU
Discovery (PMTUD) सही ढंग से काम करे।
:::

### GRE एंटी-स्पूफ़िंग (अपस्ट्रीम ACL)

GRE (IP प्रोटोकॉल 47) प्रमाणीकरण प्रदान नहीं करता। जो कोई भी
आउटर IP जोड़ी जानता है वह टनल में ट्रैफ़िक इंजेक्ट कर सकता है। अपस्ट्रीम
राउटर या फ़ायरवॉल पर ACL लागू करें ताकि इनबाउंड GRE को केवल
अपेक्षित Cloud स्क्रबिंग-सेंटर स्रोत IP तक सीमित किया जा सके:

```shell title="Cisco IOS"
! Example upstream router ACL (Cisco IOS style)
ip access-list extended ALLOW-XC-GRE
  permit gre host xXC_C1_OUTER_V4x host xBIGIP_A_OUTER_V4x
  permit gre host xXC_C2_OUTER_V4x host xBIGIP_A_OUTER_V4x
  permit gre host xXC_C1_OUTER_V4x host xBIGIP_B_OUTER_V4x
  permit gre host xXC_C2_OUTER_V4x host xBIGIP_B_OUTER_V4x
  deny   gre any host xBIGIP_A_OUTER_V4x log
  deny   gre any host xBIGIP_B_OUTER_V4x log
```

:::note
अपने राउटर प्लेटफ़ॉर्म के अनुसार सिंटैक्स को अनुकूलित करें। IPv6 GRE टनल के लिए
IPv6 ACL पर भी यही सिद्धांत लागू होता है। BIG-IP-A
(`xBIGIP_A_OUTER_V4x`) और BIG-IP-B (`xBIGIP_B_OUTER_V4x`)
दोनों आउटर IP को GRE गंतव्य के रूप में अनुमति दी जानी चाहिए।
:::

### इनर सेल्फ़-IP (BGP पीयरिंग)

इनर IP एड्रेस (GRE टनल के अंदर) असाइन करें जो Cloud के साथ
**BGP सेशन** बनाएंगे। पीयरिंग सेशन स्थापित होने के लिए `allow-service` में
`tcp:179` (BGP) शामिल होना आवश्यक है। इनर सेल्फ़ IP पर
`icmp:any` जोड़ने से टनल के माध्यम से PMTUD और रीचेबिलिटी
परीक्षण सक्षम होता है:

**Tunnel C1-T1 — BIG-IP-A से xCENTER_1x:**

```shell title="tmsh"
create net self xc-ddos-c1t1-inner-v4 \
  vlan xc-ddos-c1t1-v4 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C1_T1_INNER_V4x/30

create net self xc-ddos-c1t1-inner-v6 \
  vlan xc-ddos-c1t1-v6 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C1_T1_INNER_V6x/64
```

**Tunnel C2-T1 — BIG-IP-A से xCENTER_2x:**

```shell title="tmsh"
create net self xc-ddos-c2t1-inner-v4 \
  vlan xc-ddos-c2t1-v4 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C2_T1_INNER_V4x/30

create net self xc-ddos-c2t1-inner-v6 \
  vlan xc-ddos-c2t1-v6 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C2_T1_INNER_V6x/64
```

**Tunnel C1-T2 — BIG-IP-B से xCENTER_1x:**

```shell title="tmsh"
create net self xc-ddos-c1t2-inner-v4 \
  vlan xc-ddos-c1t2-v4 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C1_T2_INNER_V4x/30

create net self xc-ddos-c1t2-inner-v6 \
  vlan xc-ddos-c1t2-v6 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C1_T2_INNER_V6x/64
```

**Tunnel C2-T2 — BIG-IP-B से xCENTER_2x:**

```shell title="tmsh"
create net self xc-ddos-c2t2-inner-v4 \
  vlan xc-ddos-c2t2-v4 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C2_T2_INNER_V4x/30

create net self xc-ddos-c2t2-inner-v6 \
  vlan xc-ddos-c2t2-v6 \
  traffic-group traffic-group-local-only \
  allow-service add { tcp:179 icmp:any } \
  address xBIGIP_C2_T2_INNER_V6x/64
```

### BGP

Route Domain 0 के लिए BGP कॉन्फ़िगर करने हेतु [imish](https://techdocs.f5.com/kb/en-us/products/big-ip_ltm/manuals/product/big-ip-tmos-routing-administration-14-0-0/11.html) का उपयोग करें।

:::note[प्रति-यूनिट कॉन्फ़िगरेशन]
`imish` BGP कॉन्फ़िगरेशन प्रत्येक BIG-IP यूनिट के लिए स्थानीय होता है। प्रत्येक
यूनिट केवल अपनी स्वयं की टनल के लिए नेबर कॉन्फ़िगर करती है:

- **BIG-IP-A** C1-T1 और C2-T1 नेबर कॉन्फ़िगर करता है।
- **BIG-IP-B** C1-T2 और C2-T2 नेबर कॉन्फ़िगर करता है।

`router-id` प्रति यूनिट अद्वितीय होना चाहिए (प्रत्येक यूनिट का अपना आउटर
सेल्फ़ IP उपयोग करें)।
:::

1. RD 0 के लिए imish में प्रवेश करें:

   ```bash title="bash"
   tmsh run /util imish -r 0
   ```

1. प्रिविलेज्ड और कॉन्फ़िग मोड में प्रवेश करें:

   ```shell title="imish"
   localhost.localdomain[0]> enable
   localhost.localdomain[0]# configure terminal
   ```

1. उदाहरण BGP कॉन्फ़िगरेशन:

:::caution[प्लेसहोल्डर मानों को बदलें]
- `xCUSTOMER_ASNx` — ARIN (या समकक्ष RIR) के साथ पंजीकृत आपका सार्वजनिक ASN। राउटेड DDoS के लिए **सार्वजनिक ASN आवश्यक है**
  क्योंकि Cloud आपके प्रीफ़िक्स को विश्व स्तर पर एनाउंस करेगा।
  प्राइवेट ASN (64512-65534 / 4200000000-4294967294) केवल तभी
  स्वीकार्य है जब SOC आपके डिप्लॉयमेंट के लिए स्पष्ट रूप से प्राइवेट-ASN पीयरिंग का समर्थन करता हो।
- `xF5_XC_ASNx` — Cloud ASN (SOC द्वारा प्रदान किया गया)।
- `xBGP_PASSWORDx` — सहमत BGP MD5 पासवर्ड (या Console
  "default secret" से मिलान करें)। इस पासवर्ड को अन्य सेवाओं के लिए कभी पुन: उपयोग न करें।
:::

**BIG-IP-A** (router-id `xBIGIP_A_OUTER_V4x`, नेबर C1-T1 + C2-T1):

```shell title="imish"
router bgp xCUSTOMER_ASNx
  no synchronization
  bgp log-neighbor-changes
  no auto-summary
  bgp router-id xBIGIP_A_OUTER_V4x
  bgp graceful-restart restart-time 120
  redistribute kernel route-map route-to-cloud-ipv4

  neighbor cloud peer-group
  neighbor cloud remote-as xF5_XC_ASNx
  neighbor cloud description cloud-peer-group
  neighbor cloud password xBGP_PASSWORDx
  neighbor cloud timers 10 30
  neighbor cloud soft-reconfiguration inbound
  neighbor cloud version 4
  neighbor cloud capability graceful-restart
  neighbor cloud send-community
  neighbor cloud ttl-security hops 1
  neighbor cloud maximum-prefix 10 warning-only
  neighbor cloud prefix-list deny-all in
  neighbor cloud prefix-list route-to-cloud-ipv4 out

  neighbor xXC_C1_T1_INNER_V4x peer-group cloud
  neighbor xXC_C1_T1_INNER_V4x description cloud-c1-t1-v4

  neighbor xXC_C2_T1_INNER_V4x peer-group cloud
  neighbor xXC_C2_T1_INNER_V4x description cloud-c2-t1-v4

  address-family ipv6
    redistribute kernel route-map route-to-cloud-ipv6
    neighbor cloud activate
    neighbor cloud soft-reconfiguration inbound
    neighbor cloud capability graceful-restart
    neighbor cloud prefix-list deny-all6 in
    neighbor cloud prefix-list route-to-cloud-ipv6 out
    neighbor xXC_C1_T1_INNER_V6x peer-group cloud
    neighbor xXC_C1_T1_INNER_V6x description cloud-c1-t1-v6
    neighbor xXC_C2_T1_INNER_V6x peer-group cloud
    neighbor xXC_C2_T1_INNER_V6x description cloud-c2-t1-v6
  exit-address-family

ip prefix-list deny-all deny 0.0.0.0/0 le 32
ip prefix-list route-to-cloud-ipv4 permit xPROTECTED_PREFIX_V4x

ipv6 prefix-list deny-all6 deny ::/0 le 128
ipv6 prefix-list route-to-cloud-ipv6 permit xPROTECTED_PREFIX_V6x

ip route xPROTECTED_NET_V4x xPROTECTED_MASK_V4x null0 201
ipv6 route xPROTECTED_PREFIX_V6x null0 201

route-map route-to-cloud-ipv4 permit 10
  match ip address prefix-list route-to-cloud-ipv4
  set origin igp

route-map route-to-cloud-ipv6 permit 10
  match ipv6 address prefix-list route-to-cloud-ipv6
  set origin igp
```

**BIG-IP-B** (router-id `xBIGIP_B_OUTER_V4x`, नेबर C1-T2 + C2-T2):

```shell title="imish"
router bgp xCUSTOMER_ASNx
  no synchronization
  bgp log-neighbor-changes
  no auto-summary
  bgp router-id xBIGIP_B_OUTER_V4x
  bgp graceful-restart restart-time 120
  redistribute kernel route-map route-to-cloud-ipv4

  neighbor cloud peer-group
  neighbor cloud remote-as xF5_XC_ASNx
  neighbor cloud description cloud-peer-group
  neighbor cloud password xBGP_PASSWORDx
  neighbor cloud timers 10 30
  neighbor cloud soft-reconfiguration inbound
  neighbor cloud version 4
  neighbor cloud capability graceful-restart
  neighbor cloud send-community
  neighbor cloud ttl-security hops 1
  neighbor cloud maximum-prefix 10 warning-only
  neighbor cloud prefix-list deny-all in
  neighbor cloud prefix-list route-to-cloud-ipv4 out

  neighbor xXC_C1_T2_INNER_V4x peer-group cloud
  neighbor xXC_C1_T2_INNER_V4x description cloud-c1-t2-v4

  neighbor xXC_C2_T2_INNER_V4x peer-group cloud
  neighbor xXC_C2_T2_INNER_V4x description cloud-c2-t2-v4

  address-family ipv6
    redistribute kernel route-map route-to-cloud-ipv6
    neighbor cloud activate
    neighbor cloud soft-reconfiguration inbound
    neighbor cloud capability graceful-restart
    neighbor cloud prefix-list deny-all6 in
    neighbor cloud prefix-list route-to-cloud-ipv6 out
    neighbor xXC_C1_T2_INNER_V6x peer-group cloud
    neighbor xXC_C1_T2_INNER_V6x description cloud-c1-t2-v6
    neighbor xXC_C2_T2_INNER_V6x peer-group cloud
    neighbor xXC_C2_T2_INNER_V6x description cloud-c2-t2-v6
  exit-address-family

ip prefix-list deny-all deny 0.0.0.0/0 le 32
ip prefix-list route-to-cloud-ipv4 permit xPROTECTED_PREFIX_V4x

ipv6 prefix-list deny-all6 deny ::/0 le 128
ipv6 prefix-list route-to-cloud-ipv6 permit xPROTECTED_PREFIX_V6x

ip route xPROTECTED_NET_V4x xPROTECTED_MASK_V4x null0 201
ipv6 route xPROTECTED_PREFIX_V6x null0 201

route-map route-to-cloud-ipv4 permit 10
  match ip address prefix-list route-to-cloud-ipv4
  set origin igp

route-map route-to-cloud-ipv6 permit 10
  match ipv6 address prefix-list route-to-cloud-ipv6
  set origin igp
```

**प्रमुख BGP सेटिंग्स की व्याख्या:**

- **`timers 10 30`** — हर 10 सेकंड में Keepalive, होल्ड टाइम 30 सेकंड। डिफ़ॉल्ट
  (60 / 180) DDoS मिटिगेशन फ़ेलओवर के लिए बहुत धीमा है।
  दोनों पक्षों से मिलान के लिए SOC के साथ टाइमर मानों का समन्वय करें।
- **`ttl-security hops 1`** — GTSM सक्षम करता है
  ([RFC 5082](https://datatracker.ietf.org/doc/html/rfc5082))। चूँकि BGP सेशन GRE
  टनल पर सिंगल-हॉप हैं, यह इनकमिंग BGP पैकेट्स पर TTL = 255 की
  आवश्यकता करके रिमोट BGP स्पूफ़िंग को रोकता है।
- **`maximum-prefix 10 warning-only`** — गहन रक्षा सुरक्षा उपाय।
  भले ही इनबाउंड प्रीफ़िक्स-लिस्ट सभी रूट्स को अस्वीकार करती है, यह
  पीयर द्वारा अनपेक्षित रूप से प्रीफ़िक्स भेजने पर चेतावनी उत्पन्न करता है।
- **`redistribute kernel`** — रूट-मैप के माध्यम से `null0` स्टैटिक रूट्स को
  BGP में इंजेक्ट करता है। एक विकल्प स्पष्ट `network`
  स्टेटमेंट का उपयोग करना है (जैसे `network xPROTECTED_PREFIX_V4x`), जो अधिक सटीक
  हैं क्योंकि अन्य कर्नेल रूट्स की परवाह किए बिना केवल सटीक प्रीफ़िक्स ही एडवर्टाइज़ किया जाता है। दोनों तरीके काम करते हैं; लचीलेपन के लिए यहाँ सख्त रूट-मैप के साथ `redistribute kernel` दिखाया गया है।

:::tip[BFD (Bidirectional Forwarding Detection)]
यदि आपका BIG-IP संस्करण GRE टनल पर BFD का समर्थन करता है, तो
सब-सेकंड विफलता पहचान के लिए इसे सक्षम करें:

```shell
neighbor cloud fall-over bfd
```

BFD अकेले BGP keepalive टाइमर्स की तुलना में तेज़ कन्वर्जेंस प्रदान करता है।
सक्षम करने से पहले अपने BIG-IP संस्करण और SOC के साथ BFD समर्थन की पुष्टि करें।
:::

उच्चतर प्रशासनिक दूरी (201) वाले `null0` स्टैटिक रूट्स सुनिश्चित करते हैं कि प्रीफ़िक्स **कर्नेल रूटिंग टेबल** में मौजूद हों, ताकि उन्हें BGP के माध्यम से Cloud को
पुनर्वितरित किया जा सके बिना गैर-हमले की स्थितियों में सामान्य रूटिंग को प्रभावित किए। यदि संरक्षित प्रीफ़िक्स पहले से ही रूटिंग टेबल में कम प्रशासनिक दूरी वाले किसी अन्य स्रोत से मौजूद है, तो `null0` रूट सक्रिय नहीं होगा और
पुनर्वितरण विफल हो सकता है — कॉन्फ़िगरेशन के बाद `show ip route` से सत्यापित करें।
