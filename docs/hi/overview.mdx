---
title: अवलोकन
description: >-
  BIG-IP HA जोड़ी से Cloud DDoS स्क्रबिंग केंद्रों तक GRE टनल और BGP पीयरिंग
  कॉन्फ़िगर करने का परिचय।
sidebar:
  order: 1
i18n:
  sourceHash: 8a2fdcdca9db
  translator: machine
---

## Cloud GRE/BGP BIG-IP

- एक BIG-IP HA जोड़ी (कस्टमर प्रिमाइसेज इक्विपमेंट, CPE के रूप में कार्य करने वाली) से **GRE टनल** और **BGP पीयरिंग** कॉन्फ़िगर करें, प्रत्येक यूनिट के लिए स्वतंत्र टनल के साथ।
- **Cloud DDoS मिटिगेशन** स्क्रबिंग केंद्रों से **राउटेड मोड** (L3/L4) में कनेक्ट करें।

```mermaid
flowchart LR
    INET["Internet<br/>Inbound Traffic"]

    subgraph XC["F5 Distributed Cloud<br/>Global Anycast"]
        SCRUB["DDoS Scrubbing<br/>L3/L4 Mitigation"]
        DROP["Attack Traffic<br/>Dropped"]
    end

    subgraph DC["xDC_NAMEx Data Center"]
        BIGIP["BIG-IP CPE<br/>(GRE endpoint)"]
        SERVERS["DDoS-Protected Servers<br/><i>Your public IP block</i>"]
    end

    INET -->|"all traffic to your<br/>public IPs"| SCRUB
    SCRUB -->|"clean traffic"| BIGIP
    SCRUB -.->|"malicious traffic"| DROP
    BIGIP --> SERVERS
    SERVERS --> BIGIP
    BIGIP -.->|"return traffic<br/>via ISP uplink<br/>(asymmetric path)"| INET

    style SERVERS fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

## आवश्यकताएँ

- आपके टेनेंट के लिए Cloud **L3/L4 राउटेड DDoS मिटिगेशन** सेवा
  (Always On या Always Available) सक्षम होनी चाहिए।
- BIG-IP जिसमें:
    - LTM (या समकक्ष नेटवर्किंग मॉड्यूल)।
    - **डायनामिक राउटिंग (BGP)** लाइसेंस प्राप्त और सक्षम हो।
- राउटेड मोड: सुरक्षा के लिए कम से कम एक **सार्वजनिक रूप से विज्ञापित /24 (या छोटा)**
  प्रीफ़िक्स (IPv6 न्यूनतम **/48** है)।
    - संरक्षित प्रीफ़िक्स **सार्वजनिक रूप से राउटेबल होने चाहिए** (गैर-RFC 1918)।
     जब टनल सार्वजनिक इंटरनेट से गुज़रते हैं तो GRE बाहरी एंडपॉइंट भी सार्वजनिक रूप से राउटेबल होने चाहिए; प्राइवेट
     कनेक्टिविटी (L2, प्राइवेट पीयरिंग) का उपयोग करने वाले डिप्लॉयमेंट RFC 1918 एंडपॉइंट
     पतों का उपयोग कर सकते हैं।
- आपके डेटा सेंटर/राउटर और
  Cloud स्क्रबिंग केंद्र(केंद्रों) के बीच कनेक्टिविटी।

## HA आर्किटेक्चर

BIG-IP को **एक्टिव/स्टैंडबाय HA जोड़ी** के रूप में डिप्लॉय किया जाता है, प्रत्येक यूनिट
को हर स्क्रबिंग केंद्र के लिए अपने स्वतंत्र GRE टनल और BGP सेशन मिलते हैं:

```mermaid
flowchart LR
    subgraph F5XC["F5 Distributed Cloud"]
        C1["xCENTER_1x scrubbing center"]
        C2["xCENTER_2x scrubbing center"]
    end

    subgraph DC["xDC_NAMEx Data Center"]
        direction TB
        subgraph UNITA["BIG-IP-A (Active)"]
            A_C1["C1-T1 tunnel<br/>BGP Established"]
            A_C2["C2-T1 tunnel<br/>BGP Established"]
        end
        subgraph UNITB["BIG-IP-B (Standby)"]
            B_C1["C1-T2 tunnel<br/>Graceful-Restart Ready"]
            B_C2["C2-T2 tunnel<br/>Graceful-Restart Ready"]
        end
        SERVERS["DDoS-Protected Servers"]
    end

    C1 -- "GRE C1-T1" --> A_C1
    C2 -- "GRE C2-T1" --> A_C2
    C1 -- "GRE C1-T2" --> B_C1
    C2 -- "GRE C2-T2" --> B_C2
    UNITA --> SERVERS
    UNITB --> SERVERS

    style SERVERS fill:#e8f5e9,stroke:#2e7d32,stroke-width:2px
```

- **स्वतंत्र टनल एंडपॉइंट**: प्रत्येक BIG-IP यूनिट का अपना
  नॉन-फ़्लोटिंग बाहरी सेल्फ IP (`traffic-group-local-only`) और अपना
  GRE टनल का सेट होता है। BIG-IP-A `xBIGIP_A_OUTER_V4x` और
  BIG-IP-B `xBIGIP_B_OUTER_V4x` को टनल एंडपॉइंट के रूप में उपयोग करता है। यह
  टनल सोर्सिंग के लिए फ़्लोटिंग IP पर निर्भरता से बचाता है।
- **स्वतंत्र BGP सेशन**: प्रत्येक यूनिट अपने टनल पर अपने BGP सेशन चलाती है।
  BIG-IP-A C1-T1 और C2-T1 के साथ पीयर करता है;
  BIG-IP-B C1-T2 और C2-T2 के साथ पीयर करता है। फ़ेलओवर पर स्टैंडबाय
  यूनिट के BGP सेशन पहले से स्थापित होते हैं, इसलिए
  Cloud तुरंत ट्रैफ़िक शिफ़्ट कर सकता है।
- **कॉन्फ़िग सिंक**: टनल, सेल्फ IP, और राउटिंग कॉन्फ़िगरेशन
  **config-sync** के माध्यम से यूनिट्स के बीच सिंक होते हैं। चूँकि `imish`
  BGP कॉन्फ़िगरेशन प्रति-यूनिट होता है, प्रत्येक यूनिट अपने
  नेबर स्टेटमेंट बनाए रखती है। सत्यापित करें कि सिंक में सभी tmsh ऑब्जेक्ट शामिल हैं।
- **एक्टिव/स्टैंडबाय BGP व्यवहार**: एक्टिव यूनिट सामान्य BGP
  एट्रिब्यूट्स के साथ संरक्षित प्रीफ़िक्स का विज्ञापन करती है। स्टैंडबाय यूनिट
  या तो लंबे AS-path प्रीपेंड (जो इसे कम प्राथमिक बनाता है) के साथ समान प्रीफ़िक्स का
  विज्ञापन कर सकती है या फ़ेलओवर तक विज्ञापन दबा सकती है। इस दृष्टिकोण का SOC के साथ समन्वय करें।
- **फ़ेलओवर कन्वर्जेंस**: `graceful-restart` सक्षम और
  स्वतंत्र टनल के साथ, नई एक्टिव यूनिट में पहले से स्थापित
  BGP सेशन होते हैं। कन्वर्जेंस BGP बेस्ट-पाथ सिलेक्शन पर निर्भर करता है जो
  नई एक्टिव यूनिट के विज्ञापनों पर शिफ़्ट होता है। `run sys failover standby` के साथ
  परीक्षण करें।

:::note
ऊपर दिया गया स्वतंत्र-टनल HA मॉडल ग्राहक-पक्ष डिवाइस रिडंडेंसी के लिए
अनुशंसित दृष्टिकोण है। उत्पादन में जाने से पहले अपने विशिष्ट
फ़ेलओवर डिज़ाइन को अपनी अकाउंट टीम के साथ सत्यापित करें, विशेष रूप से
AS-path प्रीपेंड रणनीति और BGP पुनः कन्वर्जेंस समय के संबंध में।
:::
